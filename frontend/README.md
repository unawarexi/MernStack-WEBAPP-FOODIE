- **create a json for sidebar**
- **create a right header search**
- **create categories section**
  ### Fetch data from API
  ### Check if response data is valid
  ### create states to handle active and categories
  ### weigh ifs then setcategories to update state accordingly
  ### active category
  ### a select category fn to set it to current category
  ### map categories and set isActive logic
  ### Fetch categories data when component mounts
- **create a json for sidebar**
- **create a json for sidebar**
